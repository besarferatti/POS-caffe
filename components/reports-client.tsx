"use client";
import { useRouter, useSearchParams } from "next/navigation";
import { Download, RefreshCw } from "lucide-react";
import { Button } from "@/components/ui/button";
type Option={id:string;name:string}; type Row=Record<string, unknown>; type Dashboard={summary:Row;sales_by_day:Row[];payment_methods:Row[];products:Row[];categories:Row[];employees:Row[];tables:Row[];orders:Row[];operations:Row}; type Props={data:Dashboard | null;error?:string;canOperational:boolean;canFinancial:boolean;filters:Record<string,string>;staff:Option[];methods:Option[];categories:Option[]};
const money=new Intl.NumberFormat(undefined,{style:"currency",currency:"USD"}); const n=(v:unknown)=>money.format(Number(v||0));
function Rows({title,rows,columns}:{title:string;rows:Record<string, unknown>[];columns:[string,string][]}) { return <section className="rounded-lg border bg-card"><h2 className="border-b px-4 py-3 text-lg font-bold">{title}</h2>{rows?.length?<div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-muted/40"><tr>{columns.map(([k,l])=><th key={k} className="px-4 py-2 text-left font-medium">{l}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr className="border-t" key={i}>{columns.map(([k])=><td className="whitespace-nowrap px-4 py-2" key={k}>{k.includes("sales")||["total","paid","balance"].includes(k)?n(row[k]):String(row[k]??"—")}</td>)}</tr>)}</tbody></table></div>:<p className="p-6 text-sm text-muted-foreground">No records match the selected range and filters.</p>}</section> }
export function ReportsClient({data,error,canOperational,canFinancial,filters,staff,methods,categories}:Props){const router=useRouter(), search=useSearchParams(); const update=(form:FormData)=>{const p=new URLSearchParams(); for(const k of ["start","end","staff","method","category"]) {const v=String(form.get(k)||"");if(v)p.set(k,v)} router.push(`/app/reports?${p}`)}; const q=search.toString(); const summary=data?.summary ?? {};
return <div className="space-y-6"><header><p className="text-sm font-semibold text-primary">REPORTING</p><h1 className="text-3xl font-bold tracking-tight">Reports</h1><p className="mt-1 text-muted-foreground">Financial and operational performance from immutable order and payment records.</p></header>
<form action={update} className="grid gap-3 rounded-lg border bg-card p-4 md:grid-cols-6"><label className="text-sm">Start date<input className="mt-1 w-full rounded border p-2" name="start" type="date" defaultValue={filters.start}/></label><label className="text-sm">End date<input className="mt-1 w-full rounded border p-2" name="end" type="date" defaultValue={filters.end}/></label><label className="text-sm">Staff<select className="mt-1 w-full rounded border p-2" name="staff" defaultValue={filters.staff}><option value="">All staff</option>{staff.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label className="text-sm">Payment method<select className="mt-1 w-full rounded border p-2" name="method" defaultValue={filters.method}><option value="">All methods</option>{methods.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label className="text-sm">Category<select className="mt-1 w-full rounded border p-2" name="category" defaultValue={filters.category}><option value="">All categories</option>{categories.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><div className="flex items-end gap-2"><Button type="submit"><RefreshCw className="mr-1 size-4"/>Refresh</Button></div><div className="md:col-span-6 flex flex-wrap gap-2 text-sm"><button type="button" className="underline" onClick={()=>router.push('/app/reports')}>Today</button><button type="button" className="underline" onClick={()=>{const d=new Date();d.setDate(d.getDate()-1);const x=d.toISOString().slice(0,10);router.push(`/app/reports?start=${x}&end=${x}`)}}>Yesterday</button><button type="button" className="underline" onClick={()=>{const d=new Date();const m=new Date(d);m.setDate(d.getDate()-d.getDay());router.push(`/app/reports?start=${m.toISOString().slice(0,10)}&end=${d.toISOString().slice(0,10)}`)}}>This week</button><button type="button" className="underline" onClick={()=>{const d=new Date();router.push(`/app/reports?start=${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-01&end=${d.toISOString().slice(0,10)}`)}}>This month</button></div></form>
{error?<p role="alert" className="rounded bg-red-50 p-4 text-red-700">Unable to load reports: {error}</p>:!data?<p className="rounded border p-6 text-muted-foreground">Loading report data…</p>:<>
<div className="flex gap-2 border-b">{canFinancial&&<span className="border-b-2 border-primary px-3 py-2 font-semibold">Financial</span>}{canOperational&&<span className="px-3 py-2 font-semibold">Operational</span>}</div>
{canFinancial&&<><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[["Gross sales","gross_sales"],["Net sales","net_sales"],["Payments","total_payments"],["Cash sales","cash_sales"],["Card sales","card_sales"],["Discounts","discounts"],["Taxes","taxes"],["Tips","tips"],["Paid orders","paid_orders"],["Outstanding","outstanding_balance"],["Internal-only","internal_only_total"],["Sales today","sales_today"]].map(([l,k])=><article className="rounded-lg border bg-card p-4" key={k}><p className="text-sm text-muted-foreground">{l}</p><p className="mt-1 text-xl font-bold">{k==="paid_orders"?String(summary[k] ?? 0):n(summary[k])}</p></article>)}</div><div className="flex flex-wrap gap-2"><a href={`/app/reports/export?kind=orders&${q}`}><Button variant="outline"><Download className="mr-1 size-4"/>Orders CSV</Button></a><a href={`/app/reports/export?kind=payments&${q}`}><Button variant="outline">Payments CSV</Button></a><a href={`/app/reports/export?kind=products&${q}`}><Button variant="outline">Product sales CSV</Button></a><a href={`/app/reports/export?kind=categories&${q}`}><Button variant="outline">Category sales CSV</Button></a><a href={`/app/reports/export?kind=employees&${q}`}><Button variant="outline">Employee sales CSV</Button></a><a href={`/app/reports/export?kind=taxes&${q}`}><Button variant="outline">Tax summary CSV</Button></a><a href={`/app/reports/export?kind=internal-only&${q}`}><Button variant="outline">Internal-only CSV</Button></a></div><div className="grid gap-5 lg:grid-cols-2"><Rows title="Sales by day" rows={data.sales_by_day} columns={[["day","Day"],["sales","Sales"],["payments","Payments"]]}/><Rows title="Sales by payment method" rows={data.payment_methods} columns={[["name","Method"],["sales","Sales"],["payments","Payments"]]}/><Rows title="Product sales" rows={data.products} columns={[["name","Product"],["quantity","Quantity"],["sales","Sales"]]}/><Rows title="Category sales" rows={data.categories} columns={[["name","Category"],["quantity","Quantity"],["sales","Sales"]]}/><Rows title="Employee sales" rows={data.employees} columns={[["name","Employee"],["orders","Orders"],["sales","Sales"]]}/><Rows title="Table sales" rows={data.tables} columns={[["name","Table"],["orders","Orders"],["sales","Sales"]]}/></div></>}
{canOperational&&<><h2 className="text-2xl font-bold">Operational snapshot</h2><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(data.operations).map(([k,v])=><article key={k} className="rounded-lg border bg-card p-4"><p className="text-sm capitalize text-muted-foreground">{k.replaceAll('_',' ')}</p><p className="text-xl font-bold">{String(v)}</p></article>)}</div></>}<Rows title="Order details" rows={data.orders} columns={[["id","Order ID"],["table","Table"],["worker","Worker"],["status","Status"],["total","Total"],["paid","Paid"],["balance","Balance"]]}/></>}</div>}
